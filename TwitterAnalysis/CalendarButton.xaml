<UserControl x:Class="TwitterAnalysis.CalendarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TwitterAnalysis"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label x:Name="SelectDateLabel" Grid.Row="0" Grid.Column="0"/>
        <Button x:Name="SelectDateButton" Width="100" Grid.Column="0" Grid.Row="1" Content="Select Date" Click="SelectDateButton_Click"/>
        <Popup x:Name="CalendarPopUp">
            <Calendar x:Name="Calendar" SelectedDatesChanged="PopupCalendar_SelectedDatesChanged"></Calendar>
        </Popup>
    </Grid>
</UserControl>
